name: Testing stuff

on:
  push:
    branches:
      - github-object-testing

jobs:
  a_test_job:
    runs-on: ubuntu-latest
    env:
      JOINED_COMMIT_MESSAGES: ${{ join(github.event.commits.*.message, ', ') }}
    steps:
      - uses: actions/checkout@v4
      - name: joined array
        run: echo "$JOINED_COMMIT_MESSAGES"
      - name: env checks
        env:
          MESSAGES_CONTAIN_STRING: ${{ contains(env.JOINED_COMMIT_MESSAGES, 'Update')}}
        run: |
          env
          echo "$MESSAGES_CONTAIN_STRING"
          
